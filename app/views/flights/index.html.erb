<section id="flights-arrival_city">
  <% @flights.each do |flight| %>
    <section class="flights">
    <p><%= flight.number %></p>
    <p><%= flight.arrival_city %></p>
    <p><%= flight.departure_city %></p>
    </section>
  <% end %>
</section>
